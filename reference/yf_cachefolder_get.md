# Returns the default folder for caching

By default, yfR uses a temp dir to store files.

## Usage

``` r
yf_cachefolder_get()
```

## Value

a path (string)

## Examples

``` r
print(yf_cachefolder_get())
#> [1] "/tmp/Rtmpl7WiWC/yf_cache"
```
